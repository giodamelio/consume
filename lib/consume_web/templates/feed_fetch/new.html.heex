<h1>New Feed fetch</h1>

<%= render "form.html", Map.merge(assigns, %{action: Routes.feed_fetch_path(@conn, :create), type: :create}) %>

<span><%= link "Back", to: Routes.feed_fetch_path(@conn, :index) %></span>
